const booleana = true;
const texto = "aqui está um texto";
const flutuante = 0.02;
const inteiro = 5;

alert(
  `boolean => ${booleana}\nstring => ${texto}\nfloat => ${flutuante}\nint => ${inteiro}`
);
